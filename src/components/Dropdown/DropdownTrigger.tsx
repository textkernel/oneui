import React from 'react';
import { Trigger, DropdownMenuTriggerProps } from '@radix-ui/react-dropdown-menu';

export const DropdownTrigger = (props: DropdownMenuTriggerProps) => <Trigger asChild {...props} />;
